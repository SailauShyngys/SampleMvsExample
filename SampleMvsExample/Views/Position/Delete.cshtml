@model IEnumerable<User>
@{
    ViewData["Title"] = "Position Delete";
}
<div class="container"> 
    <h2>List of Position</h2>
    
    <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Position</th>
      <th scope="col">Actions</th>

    </tr>
  </thead>
  <tbody>
      @foreach(var user in Model)
    {
         <tr>
             <th><input title="text"> @user.id</th> 
                <td><input title="text">@user.Position?.Title</td> 
        </tr>
    }
  </tbody>
</table>
    @foreach(var user in Model)
    {
        
    }
</div>